package com.budgethelper.model.entity;

public class Event {

	/*
	* Event содержит поля:
	* 	1) ссылка на Item (уже определнный (все поля))
	* 	2) периодичность Event (информация о следующем событии (отдельная сущность по работе с периодичностью))
	*   3) ссылка на Fund
		*
	* Поведение Event:
	* 	1) инициализация полей
	*   2) расчет даты следующего события (периодичность по исходным данным (дата начала, конца, периодичность))
	* */
}
